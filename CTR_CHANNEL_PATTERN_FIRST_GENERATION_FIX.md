# CTR Channel-Pattern-First Generation Fix

Implemented two linked behaviors requested by the user:

1. **Historical thumbnail-text pattern first** — ACTIVE packaging rules now render real matching historical thumbnail examples (exact visible text + measured CTR/impressions + structural traits). Thumbnail_Agent must derive new copy from recurring channel text structure/hierarchy before using generic model copy instincts. Historical wording is not copied verbatim.
2. **Historical visual/color pattern first** — the same examples expose presenter/hero placement, layout, palette, text colors, accent, background brightness, contrast and other packaging traits. Thumbnail_Agent must derive visual/color packaging from applicable learned channel patterns before generic aesthetics.

The ACTIVE rules artifact remains the only injection boundary. CANDIDATE/REJECTED/RETIRED rows are not exposed as generation instructions. Existing medical/title/senior-clarity/renderability constraints remain higher priority.
